# ============================================
# VeTool Production Environment Configuration
# ============================================
# IMPORTANT: Replace all placeholder values before deploying!
# Copy this to .env.prod and fill in real values.
# Never commit .env.prod with real secrets to version control.

# ============================================
# Backend (.NET API)
# ============================================

ASPNETCORE_ENVIRONMENT=Production
ASPNETCORE_URLS=http://+:8080

# Database - PostgreSQL
POSTGRES_HOST=your-production-db-host.example.com
POSTGRES_PORT=5432
POSTGRES_DB=vetool_prod
POSTGRES_USER=vetool_prod_user
POSTGRES_PASSWORD=CHANGE_THIS_STRONG_DB_PASSWORD

# Alternative: Full connection string (takes precedence if set)
# POSTGRES_CONNECTION=Host=your-db-host;Port=5432;Database=vetool_prod;Username=vetool_prod_user;Password=YOUR_PASSWORD;Ssl Mode=Require

# Redis - For caching and SignalR backplane
REDIS_CONNECTION=your-redis-host.example.com:6379
# For Redis with auth: REDIS_CONNECTION=your-redis-host:6379,password=YOUR_REDIS_PASSWORD

# JWT Configuration
# Generate: openssl rand -base64 48
JWT__Issuer=vetool-api
JWT__Audience=vetool-web
JWT__Key=GENERATE_A_STRONG_256BIT_SECRET_KEY_MIN_32_CHARS_CHANGE_THIS

# JWT Cookie Settings
JWT_COOKIE_NAME=vetool_jwt
JWT_COOKIE_DOMAIN=.yourdomain.com

# Authentication Providers
AUTH_REQUIRE_EMAIL_CONFIRMATION=true
Auth__SteamApiKey=YOUR_STEAM_WEB_API_KEY
Auth__RiotApiKey=YOUR_RIOT_API_KEY

# External API Endpoints
External__Cs2PoolApiBase=https://your-cs2-api.example.com
External__ValPoolApiBase=https://your-valorant-api.example.com

# Logging (Production - less verbose)
Logging__LogLevel__Default=Warning
Logging__LogLevel__Microsoft=Error
Logging__LogLevel__Microsoft.Hosting.Lifetime=Information

# Hangfire (Background jobs)
Hangfire__DashboardEnabled=false

# ============================================
# Frontend (Next.js)
# ============================================

NODE_ENV=production

# Public API URL (browser-facing)
NEXT_PUBLIC_API_BASE=https://api.yourdomain.com/api/v1
NEXT_PUBLIC_API_BASE_URL=https://api.yourdomain.com

# Internal API URL (server-side calls within Docker)
API_BASE_INTERNAL=http://api:8080/api/v1

# SignalR WebSocket URLs
NEXT_PUBLIC_SIGNALR_URL=https://api.yourdomain.com/hubs/lobby

# NextAuth
# Generate: openssl rand -base64 32
NEXTAUTH_SECRET=GENERATE_A_RANDOM_SECRET_USE_OPENSSL_RAND_BASE64_32
NEXTAUTH_URL=https://yourdomain.com

# ============================================
# Security Checklist Before Deploying:
# ============================================
# [ ] Replace all CHANGE_THIS and YOUR_* placeholders
# [ ] Generate unique JWT__Key (openssl rand -base64 48)
# [ ] Generate unique NEXTAUTH_SECRET (openssl rand -base64 32)
# [ ] Set strong POSTGRES_PASSWORD
# [ ] Configure JWT_COOKIE_DOMAIN for your domain
# [ ] Update all URLs to your production domain
# [ ] Enable SSL/TLS for database connection
# [ ] Secure Redis connection (password/TLS)
